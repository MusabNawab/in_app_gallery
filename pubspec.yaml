name: in_app_gallery
description: "A highly customizable Flutter package for selecting images and videos from the device gallery, featuring a polished UI and media compression."
version: 1.1.0
homepage: https://github.com/MusabNawab/in_app_gallery
repository: https://github.com/MusabNawab/in_app_gallery

topics:
  - gallery
  - image-picker
  - video-picker
  - media-picker
  - compression

environment:
  sdk: ^3.12.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  path_provider: ^2.1.6
  flutter_image_compress: ^2.5.1
  photo_manager: ^3.11.0
  uuid: ^4.6.0
  video_player: ^2.13.0
  image_picker: ^1.2.3
  fluttertoast: ^10.0.0 
  permission_handler: ^13.0.2
  skeletonizer: ^3.0.0 
  hw_video_compress: ^1.0.1
  flutter_bicubic_resize: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
